{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Test matrix: declares all test scenarios and expected outcomes. Single source of truth for test orchestration.",
  "scenarios": [
    {
      "name": "happy-path",
      "description": "Normal operation: all services healthy, TLS trusted, DNS resolving",
      "profile": "infra,apps-min",
      "tests": ["smoke"],
      "expect": "pass",
      "required": true,
      "timeout": 120
    }
  ]
}
