# ========================================
# AI Services Environment Variables
# ========================================
# Copy this file to .env and fill in your values
# ========================================

# Domain Configuration
# Your base domain (e.g., example.com)
DOMAIN=example.com

# LiteLLM Configuration
# Master API key for LiteLLM programmatic access
# Generate with: openssl rand -hex 32
LITELLM_MASTER_KEY=your_litellm_master_key_here

# OAuth secret for LiteLLM <-> Authelia integration
# Must match the client_secret in Authelia's configuration
# Generate with: openssl rand -hex 32
LITELLM_OAUTH_SECRET=your_oauth_secret_here

# Authelia JWT Secret
# Must match the JWT secret in Authelia's configuration
# Generate with: openssl rand -hex 32
AUTHELIA_JWT_SECRET=your_authelia_jwt_secret_here

# Database Configuration
# PostgreSQL password for LiteLLM database
POSTGRES_PASSWORD=your_postgres_password_here

# Network Configuration
# IP address of your main VM running Caddy + Authelia
# Example: 192.168.1.100
MAIN_VM_IP=192.168.1.100

# HuggingFace Configuration
# API token for accessing gated models (e.g., Llama 3)
# Get yours at: https://huggingface.co/settings/tokens
HUGGINGFACEHUB_API_TOKEN=your_huggingface_token_here
