# Ignore local volumes directory (containers data, models, databases, etc.)
# Anchored to the repository root to avoid matching similarly named paths elsewhere.

# Secrets and sensitive data
.env
.env.local
volumes/secrets/
*.key
stackops_ed25519

# Generated configuration files (from templates)
# Now generated in ~/.config/datamancy for security
# (No longer needed in project - all configs in runtime dir)

# Legacy gitignore (kept for backwards compatibility):
configs/
configs.backup/
bootstrap_ldap.ldif

# Runtime data
volumes/
!volumes/.gitkeep

# Docker
.docker/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temporary files
*.tmp
*.bak
*.backup

#SATURN stuff
build/
downloads/
isofiles/
id_ed*
/volumes/
*.iso
# SSH keys (NEVER COMMIT)
ssh-keystore/
id_ed25519*
*.pem
!*.pem.example

# No encrypted secrets - all secrets in ~/.config/datamancy
venv/

# Test artifacts
htmlReport/