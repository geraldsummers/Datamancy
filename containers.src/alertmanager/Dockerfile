FROM prom/alertmanager:v0.28.1

# No modifications needed - sed is available in busybox (base image)
# The entrypoint script uses sed for variable substitution
# This avoids dependency issues with envsubst/gettext

# Entrypoint will be mounted from configs at runtime
