target_collection: linux_debian_docs
required_env:
  - LINUX_DEBIAN_SOURCES
notes:
  - Fetch Debian Handbook/Wiki/manpages or kernel docs
  - Convert to text, chunk, embed (via LiteLLM), upsert to Qdrant
