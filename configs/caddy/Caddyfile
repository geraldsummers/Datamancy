# Caddy Configuration for Authelia Forward Auth
# Provenance: Phase 3 - Access Control

# Global options
{
	# Enable admin API for metrics
	admin 0.0.0.0:2019
}

# Snippet for Authelia forward auth
(authelia) {
	forward_auth authelia:9091 {
		uri /api/verify?rd=https://auth.stack.local
		copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
	}
}

# Import docker-proxy generated config
import /config/caddy/Caddyfile.autogen
