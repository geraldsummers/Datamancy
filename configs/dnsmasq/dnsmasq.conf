# Resolve *.stack.local to caddy container
# Use cname to point to Docker's DNS resolution for "caddy"
cname=stack.local,caddy
cname=*.stack.local,caddy

# Forward all other queries to Docker's embedded DNS
server=127.0.0.11

# Cache size
cache-size=1000

# Log for debugging
log-queries
log-facility=-
