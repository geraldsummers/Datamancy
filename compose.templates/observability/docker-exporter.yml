# Auto-generated by build-datamancy.main.kts
# Docker container health exporter for Prometheus
# Image versions are HARDCODED at build time
# Only secrets and deployment paths use runtime ${VARS}
# NOTE: May report warnings for locally-built images (e.g., embedding-service) - this is a known issue with the exporter

services:
  docker-health-exporter:
    image: fviolence/docker-health-exporter:latest
    container_name: docker-health-exporter
    restart: unless-stopped
    networks:
      - monitoring
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9066/metrics"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
