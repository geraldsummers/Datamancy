services:
  watchtower:
    image: containrrr/watchtower:1.7.1
    container_name: watchtower
    restart: unless-stopped
    networks:
      - docker-proxy
      - internet
      - caddy
      - monitoring
    depends_on:
      docker-socket-proxy:
        condition: service_healthy
      prometheus:
        condition: service_started
      ntfy:
        condition: service_started
    environment:
      WATCHTOWER_CLEANUP: true
      WATCHTOWER_INCLUDE_RESTARTING: true
      WATCHTOWER_SCHEDULE: 0 0 4 * * *
      DOCKER_API_VERSION: 1.44
      WATCHTOWER_NOTIFICATIONS: shoutrrr
      # Note: shoutrrr ntfy service expects 'ntfy://host/topic' format, defaults to HTTPS:443
      # Since our ntfy uses HTTP:80 internally, we need to configure shoutrrr differently
      # or expose ntfy's HTTPS endpoint. For now, notifications will fail until addressed.
      WATCHTOWER_NOTIFICATION_URL: "ntfy://${NTFY_USERNAME}:${NTFY_PASSWORD}@ntfy:80/watchtower"
      WATCHTOWER_HTTP_API_METRICS: true
      WATCHTOWER_HTTP_API_TOKEN: ${WATCHTOWER_METRICS_TOKEN}
      TZ: Australia/Sydney
      DOCKER_HOST: tcp://docker-socket-proxy:2375

networks:
  docker-proxy:
    external: true
    name: datamancy_docker-proxy
  internet:
    external: true
    name: datamancy_internet
  caddy:
    external: true
    name: datamancy_caddy
  monitoring:
    external: true
    name: datamancy_monitoring
