# Auto-generated by build-datamancy.main.kts
# Volume strategy:
# - Simple named volumes for everything stored on Docker data-root (BTRFS)
# - Explicit driver_opts bind mounts ONLY for special storage locations:
#   * Qdrant: SSD for vector database performance
#   * qBittorrent: Media drive for downloads
#   * Seafile media: Media drive for large files

volumes:
  # ============================================================================
  # Standard Named Volumes (Docker-managed on BTRFS)
  # ============================================================================

  # Application Data
  bookstack_data:
  clickhouse_data:
  element_data:
  forgejo_data:
  grafana_data:
  kopia_data:
  ldap_data:
  mailserver_data:
  mariadb_data:
  onlyoffice_data:
  open_webui_data:
  planka_data:
  postgres_data:
  prometheus_data:
  radicale_data:
  redis_data:
  roundcube_data:
  vaultwarden_data:
  authelia_data:
  jupyterhub_data:
  homeassistant_config:
  synapse_data:
  ntfy_data:
  couchdb_data:

  # Configuration Volumes
  caddy_config:
  ldap_config:
  litellm_config:
  qbittorrent_config:

  # Credentials/Certificates
  caddy_data:
  dind-certs:
  dind-storage:

  # Seafile important files (on BTRFS)
  seafile_files:

  # ============================================================================
  # Previously Special Storage - Now Standard Named Volumes
  # ============================================================================

  # Qdrant - Vector database storage
  qdrant_data:

  # qBittorrent - Torrent downloads
  qbittorrent_data:

  # Seafile media - Large file storage
  seafile_media:

  # Data Fetcher - Fetched data storage
  datafetcher_data: