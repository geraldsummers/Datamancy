# Auto-generated by build-datamancy.main.kts
# Image versions are HARDCODED at build time
# Only secrets and deployment paths use runtime ${VARS}

services:
  embedding-service:
    image: datamancy/embedding-bge:latest
    build:
      context: ./src/embedding-bge
      dockerfile: Dockerfile
    container_name: embedding-service
    restart: unless-stopped
    networks:
      - ai
    environment:
      RUST_LOG: warn  # Only show warnings and errors, not INFO spam
    command: >
      --model-id /data/bge-m3
      --dtype float32
      --port 8080
      --max-concurrent-requests 512
      --max-batch-tokens 16384
      --max-batch-requests 256
      --pooling mean
    # GPU disabled - runs on CPU to avoid OOM with vllm-7b
    # Uncomment GPU section below to enable GPU
    deploy:
      resources:
        limits:
          cpus: '20.0'     # Max 20 CPU cores (adjust for your system)
        # GPU disabled - uncomment to enable:
        # reservations:
        #   devices:
        #     - driver: nvidia
        #       count: 1
        #       capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 75s
