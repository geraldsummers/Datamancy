# Auto-generated by build-datamancy.main.kts
# Local Docker registry for CI/CD image storage

services:
  registry:
    image: registry:2.8
    container_name: registry
    restart: unless-stopped
    networks:
      caddy:
        aliases:
          - registry.${DOMAIN}
      docker-proxy:
        aliases:
          - registry.${DOMAIN}
    environment:
      REGISTRY_STORAGE_FILESYSTEM_ROOTDIRECTORY: /var/lib/registry
      REGISTRY_HTTP_ADDR: 0.0.0.0:5000
      # Allow insecure access from internal network (DinD, Forgejo Actions)
      REGISTRY_HTTP_TLS_CERTIFICATE: ""
      REGISTRY_HTTP_TLS_KEY: ""
      # Explicitly disable HTTPS validation to allow insecure HTTP access
      REGISTRY_VALIDATION_DISABLED: "true"
    volumes:
      - registry_data:/var/lib/registry
    ports:
      - "5000:5000"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5000/v2/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
