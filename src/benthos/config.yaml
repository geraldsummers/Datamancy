# Benthos minimal configuration
# This is a placeholder - add your pipelines in /pipelines/*.yaml

http:
  address: 0.0.0.0:4195
  enabled: true
  root_path: /benthos
  debug_endpoints: true

# Minimal input/output to keep service running
input:
  generate:
    interval: 1h
    mapping: 'root = ""'

output:
  drop: {}

# Example: RSS to ClickHouse pipeline (disabled by default)
# See pipelines/ directory for active pipelines
