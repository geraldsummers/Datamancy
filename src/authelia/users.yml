# Simple file backend with one admin user. Add more as needed.
users:
  admin@lab.localhost:
    displayname: "Admin"
    # Generate your own hash (see below) and paste it here:
    password: "ARGON2ID_HASH_GOES_HERE"
    email: admin@lab.localhost
    groups:
      - admins
      - users
