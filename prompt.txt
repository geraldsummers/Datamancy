this here is a docker compose stack generator
global.settings/ contains templates for general settings for configuring the stack
stack.compose/ contains templates for individual services
stack.config/ contains templates for configuration files that go inside containers
stack.containers/ contains dockerfile source for building custom containers
stack.kotlin/ contains kotlin source for building custom services
tests.compose/ contains compose templates for test-only services
tests.config/ contains configuration templates for tests
tests.containers/ contains test container sources (like the test runner)
tests.kotlin/ contains test Kotlin sources (like the test runner)
build-datamancy-v3.kts is the build script
outputs in dist/
dist/docker-compose.yml is the final docker compose file
smart-up.sh (included in dist/) is a script that will only start changed containers - dont use normal up it takes waaaaay too long

the server we are testing on is accessible at latium.local
you have pubkey ssh access via ssh gerald@latium.local "command"
dont write reports - no one will read them

inside tests.containers/test-runner there is a test runner that can be used to test the containers
the compose files are in ~/datamancy on the server
the test runner help script is located at ~/datamancy/run-tests.sh
do not use -v in rsync

run the tests and report back, analyze the screenshots the test runner generates
experimentally diagnose all issues by experimenting on the server
run ALL tests - no need to update first i've just updated the server!

take initiative in your fixing
be humble
if you're going to wait, wait for 120s at a time
commit or .gitignore, the buildscript uses git for versioning so its important to keep this under control

i'm not able to reply for like 6 hours so dont stop for questions unless you absolutely have to

in that stack.kotlin dir is trading-sdk and a few other trading related things that should all be available on the server
i want you to intelligently audit and experiment and improve the trading stuff so it provides easy options to make money
it's primarily meant to be deployed in jupyter notebooks because so much of it is going to depend on server infra
the easier you can make it for me to make money with this lib the better,
add premade analysis notebooks for back testing and quantifying profitable strategies based upon real data from our market data ingestion pipeline/db
a bunch of different relevant rss sources parsed into bookstack and quantified sentiment analysis via the lm and represented in grafana
a bunch of different relevant market data pipelined in and wired into grafana charts
thorough testing is essential to ensure that the stack works as expected

another key feature that needs thorough testing is stack self awareness and introspection
the stack bundles it's own source code in a forgejo repository
the stack provides a containerized workspace via a virtualized docker socket and a llm server
the llm should be able to get context from agent-tool-server, agent-tool-server should be rewired to conform to model context protocol and renamed to model context server
any lm collecting context from the mcp server should be able to perform introspection on the stack and call all the stack tools
mcp auth should be managed via each agent needing an ldap account which determines what it gets access too

a heap of different internal systems should have easy modular wiring into grafana, home assistant for example
consider which services should be easily visualizble in grafana and configure this

account addition removal and role control need to be centralized and tested