services:
  alpha-discovery-service:
    image: datamancy/alpha-discovery-service:local-build
    build:
      context: .
      dockerfile: ./stack.containers/alpha-discovery-service/Dockerfile
    container_name: alpha-discovery-service
    restart: unless-stopped
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
    networks:
      - litellm
      - trading
      - postgres
      - monitoring
    depends_on:
      market-postgres:
        condition: service_healthy
      alpha-dataset-service:
        condition: service_started
      alpha-analytics-service:
        condition: service_started
      tx-gateway:
        condition: service_started
    environment:
      PORT: 8080
      POSTGRES_HOST: market-postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: datamancy
      POSTGRES_USER: ${POSTGRES_PIPELINE_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PIPELINE_PASSWORD}
      TX_GATEWAY_URL: http://tx-gateway:8080
      DATAMANCY_TRADING_POLICY_FILE: /app/config/trading-policy.json
    volumes:
      - ./configs/trading/trading-policy.json:/app/config/trading-policy.json:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
