[server]
hosts = 0.0.0.0:5232

[auth]
# Use http_x_remote_user for proxy authentication from Authelia
type = http_x_remote_user

[storage]
filesystem_folder = /data/collections

[web]
type = internal

[logging]
level = info
