###############################################
# Datamancy Stack - Environment Template
#
# This file documents the environment variables used by the stack.
# NEVER commit actual secrets to version control!
#
# For production:
# 1. Run: docker compose --profile bootstrap run --rm secrets-manager
# 2. Secrets are auto-generated and encrypted in volumes/secrets/
# 3. Services load secrets from the encrypted store at runtime
#
# For development:
# Copy this file to .env and set non-secret values only
###############################################

# Domain configuration (OK to commit)
DOMAIN=localhost

# Admin email (OK to commit, not sensitive)
STACK_ADMIN_EMAIL=admin@localhost

# External API tokens (provide via environment variables, not .env file)
# HUGGINGFACEHUB_API_TOKEN=<provide-via-env>

# Mail domain configuration (OK to commit)
MAIL_DOMAIN=localhost

# Vector database configuration (OK to commit)
VECTOR_EMBED_SIZE=384
QDRANT_URL=http://qdrant:6333

# LLM configuration (OK to commit)
EMBED_MODEL=embed-small
LOCALAI_URL=http://localai:8080/v1

###############################################
# SECRETS - Auto-generated, NEVER set these manually!
###############################################
# The following secrets are generated automatically by the secrets-manager service.
# Do NOT set these in .env or commit them to version control!
#
# - STACK_ADMIN_USER
# - STACK_ADMIN_PASSWORD
# - AUTHELIA_JWT_SECRET
# - AUTHELIA_SESSION_SECRET
# - AUTHELIA_STORAGE_ENCRYPTION_KEY
# - AUTHELIA_OIDC_HMAC_SECRET
# - AUTHELIA_IDENTITY_PROVIDERS_OIDC_ISSUER_PRIVATE_KEY
# - All *_OAUTH_SECRET variables
# - All *_DB_PASSWORD variables
# - All *_SECRET_KEY variables
# - All service tokens and API keys
#
# These are stored encrypted in volumes/secrets/ and loaded at runtime.
###############################################
